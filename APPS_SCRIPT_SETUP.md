# ☁️ 클라우드 동기화 설정 (Google Apps Script + 시트)

대연마루 앱을 **하나의 공유 데이터베이스**에 연결해서, 링크를 여는 모든 기기가
같은 주문을 실시간으로 보게 하는 설정입니다. 데이터는 **본인 구글 드라이브 안의
구글 시트**에 저장됩니다 — 외부 서비스도, 직원 로그인도 필요 없습니다.

소요 시간: 약 5분, 한 번만 하면 됩니다.

> ⚠️ 기존 JMCC Cafe의 URL을 재사용하면 두 카페의 주문이 한 시트에 섞입니다.
> 반드시 **대연마루 전용 시트·스크립트를 새로 만드세요.**

---

## 1단계 — 구글 시트 만들기
1. <https://sheets.new> 접속 (드라이브에 새 스프레드시트가 생성됩니다).
2. 이름을 **`대연마루 주문`** 정도로 변경합니다.

## 2단계 — 백엔드 스크립트 추가
1. 그 시트에서: **확장 프로그램 ▸ Apps Script**.
2. 샘플 코드 `function myFunction() {}` 를 삭제합니다.
3. 이 저장소의 [`apps-script/Code.gs`](apps-script/Code.gs) 내용 **전체**를 복사해
   Apps Script 편집기에 붙여넣습니다.
4. **저장** (💾) 클릭.

## 3단계 — 웹앱으로 배포
1. **배포 ▸ 새 배포** 클릭.
2. "유형 선택" 옆 톱니바퀴 ⚙ → **웹 앱** 선택.
3. 설정:
   - **설명:** `cafe`
   - **실행 계정:** **나**
   - **액세스 권한:** **모든 사용자**   ← 중요, 모든 기기가 접속하려면 필수
4. **배포** 클릭.
5. **액세스 승인** → 구글 계정 선택 → "Google에서 확인하지 않은 앱" 화면에서
   **고급 ▸ …(안전하지 않음)으로 이동** → **허용**. (본인 스크립트라 정상입니다.)
6. **웹 앱 URL**을 복사합니다. 형식:
   ```
   https://script.google.com/macros/s/AKfycb................/exec
   ```

## 4단계 — URL을 앱에 등록
[`index.html`](index.html)에서 `<script>` 상단의 이 줄을 찾아:

```js
const APPS_SCRIPT_URL = "";
```

복사한 `/exec` URL을 붙여넣습니다:

```js
const APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycb.../exec";
```

저장 후 커밋·푸시하면, GitHub Pages 재빌드 후 헤더의 표시가
**📴 로컬** → **☁️ 동기화**로 바뀌고 모든 기기가 주문을 공유합니다.

> 💡 `/exec` URL만 보내주시면 제가 대신 등록·배포해 드립니다.
> (웹앱 URL은 커밋해도 안전합니다 — 접근 제어는 위 배포 설정이 담당합니다.)

---

## 동작 방식
- **실시간 갱신:** 각 기기는 화면이 켜져 있는 동안 약 4초마다 새로고침합니다
  (Apps Script는 push가 안 되므로 폴링). 새 주문·완료 처리가 몇 초 안에
  모든 기기에 반영됩니다.
- **오프라인 대비:** URL이 없거나 연결이 안 되면 해당 기기에서 **로컬 전용**
  모드로 계속 동작하며, 표시가 ⚠️/📴로 바뀝니다.

## 나중에 스크립트를 수정하면
`Code.gs`를 고친 뒤 **URL을 바꾸지 않고** 재배포하세요:
**배포 ▸ 배포 관리 ▸ ✏ 수정 ▸ 버전: 새 버전 ▸ 배포.**

## 접근 권한 참고
"액세스 권한: 모든 사용자"는 `/exec` URL을 아는 사람은 누구나 주문을
읽고 쓸 수 있다는 뜻입니다 — 내부용 주문 패드로는 충분합니다. 나중에
잠그고 싶으면 `Code.gs`에 공유 토큰 검사를 추가하는 방법이 있습니다.
